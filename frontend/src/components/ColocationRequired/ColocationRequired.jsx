function ColocationRequired({ title, message }) {
  return (
    <div className="page__content">
      <div className="page__header">
        <div>
          <h1>{title}</h1>
          <p>{message}</p>
        </div>
      </div>
    </div>
  )
}

export default ColocationRequired
