import { default as Compiler } from '../../../compiler';
import { CompileError } from '../../types/errors';
import { UseDeclarationNode } from '../../types/nodes';
export default class UseDeclarationValidator {
    private compiler;
    private declarationNode;
    constructor(compiler: Compiler, declarationNode: UseDeclarationNode);
    validate(): CompileError[];
    private validateContext;
    private validateBody;
    private validateSpecifier;
}
