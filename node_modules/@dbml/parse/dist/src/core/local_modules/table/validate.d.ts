import { default as Compiler } from '../../../compiler';
import { CompileError } from '../../types/errors';
import { ElementDeclarationNode, ExpressionNode, ListExpressionNode } from '../../types/nodes';
import { default as Report } from '../../types/report';
import { Settings } from '../../utils/validate';
export default class TableValidator {
    private declarationNode;
    private compiler;
    constructor(compiler: Compiler, declarationNode: ElementDeclarationNode);
    validate(): CompileError[];
    private validateContext;
    private validateName;
    private validateAlias;
    private validateSettingList;
    private validateBody;
    private validateFields;
    private validateFieldSetting;
    private validateSubElements;
}
export declare function validateTableSettings(settingList?: ListExpressionNode): Report<Settings>;
export declare function validateFieldSetting(parts: ExpressionNode[]): Report<Settings>;
