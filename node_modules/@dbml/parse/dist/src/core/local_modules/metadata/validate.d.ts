import { default as Compiler } from '../../../compiler';
import { CompileError } from '../../types/errors';
import { MetadataDeclarationNode } from '../../types/nodes';
export default class MetadataValidator {
    private compiler;
    private declarationNode;
    constructor(compiler: Compiler, declarationNode: MetadataDeclarationNode);
    validate(): CompileError[];
    private validateTargetKind;
    private validateTargetName;
    private validateBody;
    private validateFields;
    private validateSubElements;
}
