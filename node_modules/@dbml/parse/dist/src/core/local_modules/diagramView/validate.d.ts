import { default as Compiler } from '../../../compiler';
import { CompileError, CompileWarning } from '../../types/errors';
import { BlockExpressionNode, ElementDeclarationNode, FunctionApplicationNode } from '../../types/nodes';
export default class DiagramViewValidator {
    private compiler;
    private declarationNode;
    constructor(compiler: Compiler, declarationNode: ElementDeclarationNode);
    validate(): {
        errors: CompileError[];
        warnings: CompileWarning[];
    };
    private validateContext;
    private validateName;
    private validateAlias;
    private validateSettingList;
    validateBody(body?: FunctionApplicationNode | BlockExpressionNode): {
        errors: CompileError[];
        warnings: CompileWarning[];
    };
    validateFields(fields: FunctionApplicationNode[]): CompileError[];
    private validateSubElements;
    private validateSubBlock;
    validateSubBlockFields(sub: ElementDeclarationNode): CompileError[];
}
