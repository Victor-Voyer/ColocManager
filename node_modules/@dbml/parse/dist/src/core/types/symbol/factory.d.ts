import { Filepath } from '../filepath';
import { NodeSymbol, NodeSymbolId, NodeSymbolIdGenerator } from '.';
export default class SymbolFactory {
    private generator;
    constructor(generator: NodeSymbolIdGenerator);
    create<T extends NodeSymbol, A>(Type: {
        new (args: A, id: NodeSymbolId, filepath: Filepath): T;
    }, args: A, filepath: Filepath): T;
}
