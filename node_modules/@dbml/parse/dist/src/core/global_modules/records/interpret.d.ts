import { default as Compiler } from '../../../compiler/index';
import { default as Report } from '../../types/report';
import { TableRecord } from '../../types/schemaJson';
import { Filepath } from '../../types/filepath';
import { RecordsMetadata } from '../../types/symbol';
export default class RecordsInterpreter {
    private compiler;
    private element;
    private metadata;
    private filepath;
    constructor(compiler: Compiler, metadata: RecordsMetadata, filepath: Filepath);
    interpret(): Report<TableRecord | undefined>;
}
