import { CompileError } from '../../types/errors';
import { SyntaxNode } from '../../types/nodes';
import { GlobalModule } from '../types';
export declare const tablePartialUtils: {
    getDuplicateError(name: string, schemaLabel: string, errorNode: SyntaxNode): CompileError;
};
export declare const tablePartialModule: GlobalModule;
