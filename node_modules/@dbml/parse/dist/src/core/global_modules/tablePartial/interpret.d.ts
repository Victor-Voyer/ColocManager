import { default as Compiler } from '../../../compiler/index';
import { default as Report } from '../../types/report';
import { SchemaElement } from '../../types/schemaJson';
import { Filepath } from '../../types/filepath';
import { TablePartialSymbol } from '../../types/symbol/symbols';
export declare class TablePartialInterpreter {
    private declarationNode;
    private compiler;
    private symbol?;
    private filepath;
    private tablePartial;
    private pkColumns;
    constructor(compiler: Compiler, symbol: TablePartialSymbol, filepath: Filepath);
    interpret(): Report<SchemaElement | SchemaElement[] | undefined>;
    private interpretName;
    private interpretSettingList;
    private interpretBody;
    private interpretSubElements;
    private interpretFields;
    private interpretColumn;
    private interpretIndexes;
    private interpretChecks;
}
