import { default as Compiler } from '../../../compiler';
import { ProgramNode } from '../../types/nodes';
import { default as Report } from '../../types/report';
export default class Binder {
    private ast;
    private compiler;
    constructor(ast: ProgramNode, compiler: Compiler);
    resolve(): Report<void>;
    private checkDuplicateRefs;
    private checkStandaloneRefs;
    private checkInlineRefs;
    private checkTablePartialSelfRefs;
    /**
     * Check that no table appears in more than one TableGroup.
     */
    private checkTableGroups;
}
