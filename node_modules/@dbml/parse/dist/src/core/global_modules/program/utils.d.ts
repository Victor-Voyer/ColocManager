import { Database, ElementRef } from '../../types/schemaJson';
import { NodeSymbol } from '../../types/symbol';
export declare function pushExternal(db: Database, member: NodeSymbol, ref: ElementRef): void;
