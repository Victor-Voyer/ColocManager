import { default as Compiler } from '../../../compiler/index';
import { Filepath } from '../../types/filepath';
import { default as Report } from '../../types/report';
import { SchemaElement } from '../../types/schemaJson';
import { ProgramSymbol } from '../../types/symbol';
export default class ProgramInterpreter {
    private compiler;
    private programSymbol;
    private programNode;
    private filepath;
    private errors;
    private warnings;
    private db;
    constructor(compiler: Compiler, symbol: ProgramSymbol, filepath: Filepath);
    interpret(): Report<SchemaElement | SchemaElement[] | undefined>;
    private interpretAllSymbols;
    private interpretUseSymbol;
    private interpretAllMetadata;
    private interpretAllAliases;
    private validateRecords;
    private collectPartialRefs;
    private pushElement;
}
