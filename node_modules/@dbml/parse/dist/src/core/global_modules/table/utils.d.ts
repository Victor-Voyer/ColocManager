import { SyntaxNode } from '../../types/nodes';
import { ColumnType, InlineRef } from '../../types/schemaJson';
export declare function interpretColumnType(typeNode?: SyntaxNode): ColumnType;
export declare function interpretInlineRefs(refs: any[]): InlineRef[];
