import { default as Compiler } from '../../../compiler';
import { UseSpecifierNode } from '../../types/nodes';
import { UseSymbol } from '../../types/symbol';
import { GlobalModule } from '../types';
export declare const useUtils: {
    visibleName(compiler: Compiler, nodeOrSymbol: UseSpecifierNode | UseSymbol): string[] | undefined;
};
export declare const useModule: GlobalModule;
