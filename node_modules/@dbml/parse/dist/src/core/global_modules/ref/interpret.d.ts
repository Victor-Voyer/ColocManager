import { Ref } from '../../types/schemaJson';
import { RefMetadata, Filepath } from '../../types';
import { default as Compiler } from '../../../compiler';
import { default as Report } from '../../types/report';
export declare class RefInterpreter {
    private compiler;
    private metadata;
    private declarationNode;
    private filepath;
    private ref;
    constructor(compiler: Compiler, metadata: RefMetadata, filepath: Filepath);
    interpret(): Report<Ref>;
    private interpretName;
    private interpretBody;
}
