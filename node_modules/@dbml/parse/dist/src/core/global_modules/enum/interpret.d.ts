import { Enum } from '../../types/schemaJson';
import { EnumSymbol, Filepath } from '../../types';
import { default as Compiler } from '../../../compiler';
import { default as Report } from '../../types/report';
export declare class EnumInterpreter {
    private compiler;
    private symbol;
    private declarationNode;
    private enum;
    private filepath;
    constructor(compiler: Compiler, symbol: EnumSymbol, filepath: Filepath);
    interpret(): Report<Enum>;
    private interpretName;
    private interpretBody;
}
