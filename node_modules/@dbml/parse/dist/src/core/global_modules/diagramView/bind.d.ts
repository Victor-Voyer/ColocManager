import { default as Compiler } from '../../../compiler';
import { CompileError } from '../../types/errors';
import { ElementDeclarationNode } from '../../types/nodes';
import { SyntaxToken } from '../../types/tokens';
export default class DiagramViewBinder {
    private compiler;
    private declarationNode;
    constructor(compiler: Compiler, declarationNode: ElementDeclarationNode & {
        type: SyntaxToken;
    });
    bind(): CompileError[];
    private bindBody;
    private bindSubElements;
    private bindTableReferences;
    private bindNoteReferences;
    private bindTableGroupReferences;
    private bindSchemaReferences;
}
