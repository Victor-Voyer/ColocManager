import { default as Compiler } from '../../../compiler';
import { ElementDeclarationNode } from '../../types/nodes';
import { default as Report } from '../../types/report';
import { Project } from '../../types/schemaJson';
import { Filepath } from '../../types/filepath';
export declare class ProjectInterpreter {
    private compiler;
    private declarationNode;
    private filepath;
    private project;
    constructor(compiler: Compiler, declarationNode: ElementDeclarationNode, filepath: Filepath);
    interpret(): Report<Project>;
    private interpretName;
    private interpretSubElement;
    private interpretBody;
}
