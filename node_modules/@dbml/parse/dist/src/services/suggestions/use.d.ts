import { default as Compiler } from '../../compiler';
import { Filepath } from '../../core/types/filepath';
import { SyntaxToken } from '../../core/types/tokens';
import { CompletionList, TextModel } from '../types';
export declare function suggestUseCompletion(compiler: Compiler, filepath: Filepath, offset: number, bOcToken: SyntaxToken | undefined, model: TextModel): CompletionList | null;
