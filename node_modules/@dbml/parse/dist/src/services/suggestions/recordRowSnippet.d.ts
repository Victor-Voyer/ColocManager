import { default as Compiler } from '../../compiler';
import { Filepath } from '../../core/types/filepath';
import { CompletionList, Position, TextModel } from '../types';
export declare function suggestRecordRowSnippet(compiler: Compiler, model: TextModel, position: Position, filepath: Filepath, offset: number): CompletionList | null;
