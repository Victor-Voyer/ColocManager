import { SyntaxNode } from '../core/types/nodes';
import { SyntaxToken } from '../core/types/tokens';
import { Position, Range, TextModel } from './types';
export declare function getOffsetFromMonacoPosition(model: TextModel, position: Position): number;
export declare function getEditorRange(model: TextModel, nodeOrToken: SyntaxNode | SyntaxToken): Range;
