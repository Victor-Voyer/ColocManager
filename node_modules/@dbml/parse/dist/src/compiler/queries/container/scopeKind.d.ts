import { Filepath } from '../../../core/types/filepath';
import { default as Compiler } from '../../index';
import { ScopeKind } from '../../types';
export declare function containerScopeKind(this: Compiler, filepath: Filepath, offset: number): ScopeKind;
