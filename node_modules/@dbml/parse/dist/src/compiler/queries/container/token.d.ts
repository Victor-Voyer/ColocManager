import { Filepath } from '../../../core/types';
import { SyntaxToken } from '../../../core/types/tokens';
import { default as Compiler } from '../../index';
export declare function containerToken(this: Compiler, filepath: Filepath, offset: number): {
    token: SyntaxToken;
    index: number;
} | {
    token: undefined;
    index: undefined;
};
