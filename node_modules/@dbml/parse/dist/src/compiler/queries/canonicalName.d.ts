import { default as Report } from '../../core/types/report';
import { Filepath } from '../../core/types/filepath';
import { NodeSymbol } from '../../core/types/symbol';
import { default as Compiler } from '../index';
export interface CanonicalName {
    schema: string;
    name: string;
}
export declare function canonicalName(this: Compiler, filepath: Filepath, symbol: NodeSymbol): Report<CanonicalName | undefined>;
